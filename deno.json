{
  "tasks": {
    "check": "deno check .",
    "lint": "deno lint",
    "test": "deno test --allow-read --coverage=coverage",
    "test:e2e": "deno test --allow-all --allow-env --allow-net --allow-read --allow-write --allow-run src/*e2e_test.ts",
    "coverage": "deno coverage coverage",
    "build": "deno run -A build.ts",
    "fmt": "deno fmt",
    "all": "deno task check && deno task lint && deno task test && deno task build && deno task fmt"
  },
  "fmt": {
    "exclude": ["dist", "*.md"]
  },
  "lint": {
    "exclude": ["dist"]
  },
  "compilerOptions": {
    "lib": ["dom", "dom.iterable", "deno.window"],
    "strict": true,
    "types": ["npm:@types/chrome"]
  },
  "imports": {
    "@std/testing": "jsr:@std/testing@^1.0.16",
    "deno_emit": "https://deno.land/x/emit@0.40.0/mod.ts",
    "std/": "https://deno.land/std@0.224.0/",
    "assert/": "https://deno.land/std@0.208.0/assert/",
    "deno-dom": "jsr:@b-fuze/deno-dom",
    "puppeteer": "npm:puppeteer@^23.0.0",
    "puppeteer-core": "npm:puppeteer-core@^23.0.0"
  }
}
